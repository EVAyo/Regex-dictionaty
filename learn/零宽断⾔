零宽断⾔ ：查找在某些内容(不包括这些内容)之前或之后的东西

匹配exp前⾯的位置 ：(?=exp) 
例子：
匹配以ing结尾的单词的前半部分
\b\w+(?=ing\b) 

匹配exp后⾯的位置 ：(?<=exp) 
例子：
匹配以re开头的单词的后半部分
(?<=\bre)\w+\b


匹配后⾯跟的不是exp的位置 ：(?!exp) 
例子：匹配给大金额数字标逗号位置
 ((?<=\d)\d{3})+\b

匹配前⾯不是exp的位置 ：(?<!exp) 
